import {actions} from "./boards.actions";
import {mutations} from "./boards.mutations";
import {getters} from "./boards.getters";

export default {
	state: {
		boards: []
	},
	mutations,
	actions,
	getters
}